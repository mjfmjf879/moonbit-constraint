# MoonBit 约束求解器项目申报书
项目名称：MoonBit 约束求解器（MoonBit Constraint Solver）
项目标识：moonbit-constraint
GitHub：https://github.com/mjfmjf879/moonbit-constraint
GitLink：https://gitlink.org.cn/mjfmjf/moonbit-constraint

## 一、项目目标与场景
本项目拟构建面向 MoonBit 生态的有限域约束编程库，解决排班、资源分配、数独、配置生成和组合优化中“变量取值必须同时满足多项规则”的建模与求解问题。
与为单一场景定制的脚本相比，约束模型可以复用变量、域和规则，便于在不同业务中扩展约束，并为后续优化和增量求解保留接口。

## 二、交付范围
第一阶段交付整数区间域、命名变量、等式与不等式约束、AllDifferent、Sum、Element、可行性传播、MRV 启发式搜索、单解与多解求解 API。
项目提供 MoonBit 库、可运行命令行示例、黑盒测试、API 接口文件、README、许可证、来源说明和跨平台 CI。
使用者可以通过 MoonBit API 创建模型、添加变量和约束，再读取一个或多个结构化解；项目不绑定特定 UI 或业务框架。

## 三、技术路线
采用纯 MoonBit 实现有限域和求解器状态，以数组保存候选值，通过约束检查保证部分赋值的一致性。
搜索阶段使用确定性的 MRV 选择剩余域最小的变量；Sum 使用域上下界进行前向检查，Element 检查索引与结果域的可行配对。
后续版本将增加表约束、重启策略、冲突解释、优化目标、增量求解、调度建模辅助 API，并探索 SAT/SMT 互操作。

## 四、测试与里程碑
当前版本已覆盖域操作、AllDifferent、算术约束、Element、可满足与不可满足模型，并提供可直接运行的示例。
开发阶段持续执行 moon check、moon test、moon fmt 和 moon info；CI 覆盖 Ubuntu、macOS、Windows 及 MoonBit 可用后端。
下一阶段将加入排班和数独实例、性能基准、更多传播规则及稳定的公开 API，随后准备发布到 mooncakes.io。

## 五、原创与合规说明
项目为原创 MoonBit 实现，不复制第三方求解器源码、生成代码或测试数据；算法名称和约束编程思想属于公开技术概念。
代码与文档采用 Apache-2.0 许可证，外部资料、基准实例或参考实现如有引入，将在来源说明中记录链接、许可证和使用范围。
